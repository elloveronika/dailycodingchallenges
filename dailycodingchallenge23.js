function countBy(x, n) {
    var z = [];
    
      for (let i = 1 ; i <= n ; i++){
        z.push(i * x)
      }
        
  
    return z;
  }

  //multiply each number in the array by same number in the amount of times as n